programa
    declare tentativa texto, segredo texto.
    segredo := "segredo".
    tentativa := "".

    enquanto (tentativa != segredo) {
        escreva("Tente adivinhar o segredo!").
        leia(tentativa).
    }

    escreva("Parabens!").
fimprog.
